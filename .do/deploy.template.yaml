spec:
 name: coffeespace-mixpanel-proxy
 services:
 - name: coffeespace-mixpanel-proxy
   git:
     branch: master
     repo_clone_url:https://github.com/Pr1yansh1/Mixpanel.git
   dockerfile_path: ./Dockerfile
   http_port: 80
